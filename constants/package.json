{
  "name": "@shared/constants",
  "private": true,
  "main": "dist/index.js",
  "devDependencies": {
    "@shared/interfaces": "*"
  },
  "scripts": {
    "build": "tsc --build --clean && tsc --build",
    "lint:fix": "eslint --fix ./ --ext .ts",
    "lint": "eslint ./ --ext .ts"
  }
}
