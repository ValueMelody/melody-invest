{
  "name": "@shared/constants",
  "private": true,
  "main": "dist/index.js",
  "devDependencies": {
    "@shared/interfaces": "*"
  },
  "scripts": {
    "build": "tsc --build --clean && tsc --build"
  }
}
