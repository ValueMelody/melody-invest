{
  "name": "@shared/constants",
  "private": true,
  "main": "dist/index.js",
  "devDependencies": {
    "@shared/interfaces": "*"
  },
  "scripts": {
    "build": "rm -rf ./dist && tsc --build",
    "lint:fix": "eslint --fix ./ --ext .ts",
    "lint": "eslint ./ --ext .ts"
  }
}
