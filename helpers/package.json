{
  "name": "@shared/helpers",
  "private": true,
  "main": "dist/index.js",
  "dependencies": {
    "@shared/constants": "*",
    "crypto-js": "^4.1.1"
  },
  "devDependencies": {
    "@shared/interfaces": "*",
    "@types/crypto-js": "^4.0.2"
  },
  "scripts": {
    "build": "tsc --build --clean && tsc --build",
    "lint:fix": "eslint --fix ./ --ext .ts",
    "lint": "eslint ./ --ext .ts"
  }
}
